# Never commit real secrets. Copy this file to .env only on your machine/hosting provider.
GRAMAI_SECRET=replace-with-a-long-random-secret
GRAMAI_PAYMENT_DEMO=1
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxx
RAZORPAY_KEY_SECRET=xxxxxxxxx
RAZORPAY_WEBHOOK_SECRET=xxxxxxxxx

# Optional production providers
GRAMAI_SMTP_HOST=
GRAMAI_SMTP_PORT=587
GRAMAI_SMTP_USER=
GRAMAI_SMTP_PASSWORD=
GRAMAI_SMS_PROVIDER=DEMO

# GRAM Saathi assistant (Groq). Get a key at https://console.groq.com/keys
# Server-side only - never put this in static/ or commit a real value.
GROQ_API_KEY=
GROQ_MODEL=openai/gpt-oss-120b,qwen/qwen3.8-27b

# GRAM Saathi voice input (speech-to-text). Uses the same GROQ_API_KEY above -
# no separate key needed. Only change this if you want a different Whisper
# model than the default.
GROQ_STT_MODEL=whisper-large-v3

# GRAM Saathi voice output (text-to-speech) uses gTTS, which is free and
# key-less - no env var required.

# India-wide crop market price comparison (AGMARKNET via data.gov.in).
# Free key: https://data.gov.in -> Sign Up/Login -> My Account -> API Key.
# Dataset used: "Current Daily Price of Various Commodities from Various
# Markets (Mandi)" (resource id 9ef84268-d588-465a-a308-a864a43d0070).
# Without this key, GET /api/market-prices returns "temporarily unavailable"
# rather than inventing a price.
DATA_GOV_IN_API_KEY=

# WhatsApp Cloud API (Meta). Server-side only - never commit a real token.
WHATSAPP_ACCESS_TOKEN=
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_VERIFY_TOKEN=kisansetu_webhook_2026
WHATSAPP_GRAPH_VERSION=v23.0

# Telegram bot (get a token from @BotFather). Server-side only - never commit
# a real token. TELEGRAM_WEBHOOK_SECRET is optional but recommended: Telegram
# echoes it back in the X-Telegram-Bot-Api-Secret-Token header on every
# webhook call so you can reject spoofed requests.
TELEGRAM_BOT_TOKEN=
TELEGRAM_WEBHOOK_SECRET=
